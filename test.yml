- name: Scalr Automated Testing
  connection: local
  hosts: localhost
  vars_files:
   - vars.yml
  tasks:
# This Launches the Automated Testing farm
   - name: Launch Farm
     shell: /usr/local/bin/scalr-ctl --config /tmp/testing_creds.yml farms list
     register: farmlaunch
     ignore_errors: True
